# `x-omitempty`

Force the presence of the JSON tag `omitempty` on a field.

## Overview

In a case that you may want to add the JSON struct tag `omitempty` to types that don't have one generated by default - for instance a required field - you can use the `x-omitempty` extension.

## Example

```yaml
--8<-- "extensions/xomitempty/api.yaml"
```

## Generated Code

From here, we now get two different models:

```go
--8<-- "extensions/xomitempty/gen.go:10:13"
```

```go
--8<-- "extensions/xomitempty/gen.go:19:22"
```

## Full Example

You can see this in more detail in [the example code](https://github.com/doordash-oss/oapi-codegen-dd/tree/main/examples/extensions/xomitempty/){:target="_blank"}.

## Related Extensions

- [`x-go-type-skip-optional-pointer`](x-go-type-skip-optional-pointer.md) - Do not add a pointer type for optional fields
- [`x-go-json-ignore`](x-go-json-ignore.md) - Ignore fields when (un)marshaling JSON

