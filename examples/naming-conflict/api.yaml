openapi: "3.0.0"
info:
  version: 1.0.0
  title: naming-conflict
components:
  schemas:
    source:
      type: object
      properties:
        credit_transfer:
          $ref: '#/components/schemas/source_type'

    source_type:
      properties:
        name:
          nullable: true
          type: string
        address:
          nullable: true
          type: string

    type:
      type: string
      enum:
        # prefix will be added to avoid conflict
        - source_type
        - debit
